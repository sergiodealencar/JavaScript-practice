const mensagem = 'Olá, mundo!';

for (let indice = 0; indice < mensagem.length; indice++) {
    console.log(mensagem[indice]);
}

// Dá mesmo resultado:
// for (const caracter of mensagem) {
//  console.log(caracter);
//}